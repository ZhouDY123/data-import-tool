# 店铺数据看板：外部订单接收接口

## 1. 用途

本接口用于接收京东、天猫、拼多多店铺应用服务端推送的订单数据。

- 京东数据写入店铺一。
- 天猫数据写入店铺二。
- 拼多多数据写入店铺三。
- 原始订单 JSON 会完整保存到独立表 `external_order_records`。
- 大屏会将该表与原有订单表 `order_records` 合并，并按北京时间成交时间倒序展示。

> 请从应用服务端调用接口。接口不校验密钥或签名，请勿公开接口地址。

## 2. 线上接口地址

| 平台 | 地址 |
|---|---|
| 京东 | `https://shopview.sanqifz.com:33033/api_ingest_jd.php` |
| 天猫 | `https://shopview.sanqifz.com:33033/api_ingest_tmall.php` |
| 拼多多 | `https://shopview.sanqifz.com:33033/api_ingest_pdd.php` |

所有接口仅支持 `POST`，请求体类型为 `application/json`。

## 3. 身份校验

接口不校验密钥或签名。请求仅需使用 `POST` 并携带 `Content-Type: application/json`。

> 风险提示：任何知道接口地址的人都可以提交订单数据。请仅在受信任的应用服务端保存和调用接口地址。

## 4. 请求数据格式

```json
{
  "orders": [
    {
      "orderNo": "平台订单号",
      "paidAt": "2026-09-07 14:30:00",
      "sku": "SKU-001",
      "name": "商品名称",
      "amountFen": 6990,
      "quantity": 1,
      "region": "浙江",
      "categoryNo": 4,
      "dataType": "real"
    }
  ]
}
```

`orders` 必须为数组。一个订单含多个商品时，请为每个商品提交一条订单项，并保证 `orderNo + sku` 唯一。

### 字段说明

| 字段 | 必填 | 类型 | 说明 |
|---|---:|---|---|
| `orderNo` | 是 | string | 平台订单号 |
| `paidAt` | 是 | string / timestamp | 成交时间。推荐北京时间格式 `YYYY-MM-DD HH:mm:ss`；也支持 Unix 秒或毫秒时间戳 |
| `sku` | 否 | string | SKU；缺省时系统自动生成 |
| `name` | 否 | string | 商品名称 |
| `amountFen` | 建议 | integer | 成交金额，单位：分 |
| `amount` | 否 | number | 成交金额，单位：元；仅在未提供 `amountFen` 时使用 |
| `quantity` | 否 | integer | 数量，默认 `1` |
| `region` | 否 | string | 地区/省份，默认 `—` |
| `categoryNo` | 否 | integer | 分类编号，默认 `4` |
| `dataType` | 否 | string | 数据类型：`real` 为真实数据，`virtual` 为虚拟数据；未传时默认 `real` |

兼容字段：接口同时识别 `order_no`、`order_sn`、`tid`、`paid_at`、`pay_time`、`sku_id`、`goods_name`、`goods_quantity`、`gmv_fen` 等常见平台字段。

## 5. 成功响应

HTTP `200`：

```json
{
  "ok": true,
  "received": 1,
  "skipped": 0
}
```

| 字段 | 说明 |
|---|---|
| `received` | 已写入或更新的订单项数 |
| `skipped` | 因缺少订单号或成交时间等原因跳过的订单项数 |

接口以“平台 + 店铺 + 订单号 + SKU”去重；重复推送会更新已有记录，不会重复累计。

## 6. 错误响应

| HTTP 状态 | `error` | 说明 |
|---:|---|---|
| 400 | `invalid_json` | 请求体不是有效 JSON |
| 400 | `json_object_required` | JSON 根节点不是对象 |
| 400 | `orders_array_required` | 缺少 `orders` 数组 |
| 405 | `POST required` | 使用了非 POST 方法 |

## 7. PHP 调用示例

```php
$url = 'https://shopview.sanqifz.com:33033/api_ingest_tmall.php';
$body = json_encode(['orders' => [[
    'orderNo' => 'TM202609070001',
    'paidAt' => '2026-09-07 14:30:00',
    'sku' => 'SKU-001',
    'name' => '示例商品',
    'amountFen' => 6990,
    'quantity' => 1,
    'region' => '浙江',
]], JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES);
$ch = curl_init($url);
curl_setopt_array($ch, [
    CURLOPT_POST => true,
    CURLOPT_POSTFIELDS => $body,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
    ],
    CURLOPT_RETURNTRANSFER => true,
]);
$response = curl_exec($ch);
curl_close($ch);
```

## 8. 配置步骤

1. 登录数据看板后台，进入“外部订单接口”复制地址。
2. 将对应平台的接口地址配置到各自的应用服务端。
3. 使用一条测试订单调用接口，确认返回 `ok: true`。
4. 刷新对应店铺大屏，订单会按成交时间与原数据混合展示。
